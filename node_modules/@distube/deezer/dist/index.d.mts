import { InfoExtractorPlugin, ResolveOptions, Song, Playlist } from 'distube';

declare class DeezerPlugin extends InfoExtractorPlugin {
    validate(url: string): boolean;
    resolve<T>(url: string, options: ResolveOptions<T>): Promise<Song<T> | Playlist<T>>;
    createSearchQuery<T>(song: Song<T>): string;
    getRelatedSongs(): never[];
}

export { DeezerPlugin, DeezerPlugin as default };
